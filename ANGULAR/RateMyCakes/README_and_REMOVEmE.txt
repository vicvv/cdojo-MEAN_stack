1. Rename server/models/model.js to match your table name in the database
2. Rename and modify staff inside the server/models/model.js to match your table fields
3. Update router.js and controller.js appropriatelly

cd angular; ng build --watch